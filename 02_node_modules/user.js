const me = {
  name: "sushil",
  age: 20,
}

const sally = {
  name: "sally",
  age: 22,
}
const pet = {
  name: "dog",
  color: "white",
}
function printUser(user) {
  console.log(`name is ${user.name} and age is ${user.age}`)
}
module.exports = {
  // in node module we export all the variable and function in this way.
  //we can export a single or multiple variable or function.
  //In node modules thre is no concept of the default or named export , We have to export everything using the objector array(mostly object.)
  myName: me,
  sallyName: sally,
  printUserName: printUser,
}
